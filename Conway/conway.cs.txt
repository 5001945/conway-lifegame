메모

줄 : 내용

5 : enum은 C와 C++과 동일하다.
15,17 : internal은 같은 .cs 파일 내에서만 사용할 수 있는 한정자이다. 또한 nested class 선언으로 위계구조를 볼 수 있다(슬프게도 바깥 scope의 변수를 직접 볼 수는 없다).
21 : 클래스 인스턴스 배열을 만들고 할당할 때, 배열만 new로 할당하면 모든 배열 내 원소는 null을 가리킨다. 이를 이용해 이 배열 원소가 실제 Cell을 가리키는 게 맞는지를 확인할 수 있다.
27 : LivingCellCount 프로퍼티는 읽기 전용이며, 읽을 때마다 주위 살아있는 Cell을 Count하여 그 값을 반환한다.
45,46 : readonly는 const와 달리, 선언할 때뿐 아니라 생성자에서도 초기화할 수 있다.
56-58 : 21줄과 다르게 모든 배열 원소를 할당해주고 싶다면, for문을 이용해야 한다. C++처럼 assign(n, Cell())과 같이 하면 좋을 텐데 여기서는 클래스 인스턴스가 reference type이니 어쩔 수 없다. 
108 : [,] 연산자 오버로딩이라고 생각하면 된다.
